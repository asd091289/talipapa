import { ReactElement } from "react";

import LandingPage from "./screens/landing-page";

const App = (): ReactElement => {
  return (
    <>
      <LandingPage />
    </>
  );
};

export default App;
