import { ReactElement } from "react";

import Grid from "@mui/material/Grid";

export const ProductSection = (): ReactElement => {
  return (
    <>
      <Grid>
        <Grid>{/* <Typography> Products </Typography> */}</Grid>
      </Grid>
    </>
  );
};

export default ProductSection;
